<%= form_for(@parent_resource) do |f| %>
  <% if @parent_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parent_resource.errors.count, "error") %> prohibited this parent_resource from being saved:</h2>

      <ul>
      <% @parent_resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :prop %><br />
    <%= f.text_field :prop %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
